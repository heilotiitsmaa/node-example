import os from "os"; //info about my system (cpu or memory usage)



//userInfo()
console.log(os.userInfo());

//totamemory
console.log(os.totalmem());

//freemem()
console.log(os.freemem());

//cpus()
console.log(os.cpus()); //ocject for every core on the system